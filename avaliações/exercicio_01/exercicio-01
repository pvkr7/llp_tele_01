#include <stdio.h>

int main() {
    int x;

    printf("Digite a pontuação obtida pelo jogador: ");
    scanf("%d", &x);

    if (x < 10) {
        printf("Insuficiente\n");
    } else if (x < 40) {
        printf("Regular\n");
    } else if (x < 70) {
        printf("Bom\n");
    } else if (x < 90) {
        printf("Ótimo\n");
    } else if (x <= 100) {
        printf("Excelente\n");
    } else {
        printf("Pontuação inválida\n");
    }

    return 0;
}
#include <stdio.h>

int main() {
    int soma = 0;

    printf("Calculando a soma dos 10 primeiros números positivos...\n");

    for (int i = 1; i <= 10; i++) {
        soma += i;
    }

    printf("A soma dos 10 primeiros números positivos é: %d\n", soma);

    return 0;
}